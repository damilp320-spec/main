# 🧠 Mythera AI Hub

**Стильное десктоп-приложение для Windows 11, которое устанавливает, настраивает и запускает автономных AI-агентов прямо на вашем ПК.**

Все нейросети работают **локально** (через [Ollama](https://ollama.com)), могут **полностью взаимодействовать с компьютером** (PowerShell, файлы, приложения), имеют **доступ в интернет** (поиск, загрузка страниц), выполняют задачи по **расписанию** и понимают **голосовые команды**.

![tech](https://img.shields.io/badge/Electron-33-7c5cff) ![platform](https://img.shields.io/badge/Windows-11-29d3c2) ![local](https://img.shields.io/badge/100%25-локально-3ddc84)

## ⬇️ Скачать в один клик

### **[⬇️ Скачать для Windows (последний релиз)](https://github.com/damilp320-spec/main/releases/latest)**

[![Download](https://img.shields.io/badge/Download-Windows%20installer-7c5cff?style=for-the-badge&logo=windows)](https://github.com/damilp320-spec/main/releases/latest)

Одна кнопка — установщик `.exe` (NSIS) или portable-версия с [последнего релиза](https://github.com/damilp320-spec/main/releases/latest). Также есть [страница загрузки (GitHub Pages)](https://damilp320-spec.github.io/main/) с большой кнопкой (включается в Settings → Pages → ветка, папка `/docs`). Релизы собираются автоматически при пуше тега `v*`.

---

## 🆕 Новое: браузер, звук, умный дом и режим профи

- 🎵 **Музыка в браузере** — «включи …» открывает трек/поиск в Spotify, Apple Music, YouTube Music, **Яндекс Музыке, VK, Звук**, SoundCloud, Deezer (без встроенного плеера; сервис по умолчанию настраивается). Плюс «открой YouTube/Ozon/…» и веб-поиск.
- 🔊 **Громкость системы** — точная установка/изменение/mute (Windows Core Audio API; на Linux/mac — pactl/osascript). Слайдер в настройках и голосом: «сделай громче на 10».
- 🏠 **Умный дом — все протоколы** — Home Assistant (REST), **MQTT/Zigbee2MQTT** (нативная публикация), **Яндекс Умный дом (Алиса)**, **SberDevices (Салют)**, Tuya, Xiaomi/Aqara, Google Home, HomeKit, Matter, Tasmota, ESPHome, Sonoff/eWeLink, IFTTT, универсальный вебхук. Секреты шифруются (`safeStorage`).
- 🧲 **Сортировка вкладок** — перетаскивайте пункты бокового меню, порядок сохраняется.
- 🛠️ **Режим «полного контроля» (для профи)** — раздел «Разработчику»: сырые параметры Ollama (top_p, top_k, num_ctx, repeat_penalty, seed, num_predict, mirostat, stop), включение/отключение отдельных инструментов агента, сырой вызов модели и **редактор всего конфига в JSON** (экспорт/импорт).
- ✨ **UX-мелочи** — фокус-режим `Ctrl+B` (скрыть меню), быстрый запуск музыки и mute из командной палитры `Ctrl+K`, кнопка «Скачать последнюю версию» прямо в приложении.

## ✨ Возможности

| Раздел | Что делает |
|---|---|
| 🏠 **Главная** | Дашборд: статус Ollama, модели, агенты, нагрузка системы. Кнопка «Быстрая установка». |
| ⬇️ **Установка ИИ** | Каталог локальных моделей с автоподбором под ваше «железо». Установка/удаление в один клик с прогрессом. |
| 🤖 **Агенты** | Чат с автономными агентами, которые сами вызывают инструменты (команды, файлы, поиск, серверы, перевод). Шаблоны + конструктор. Кнопка 🧠 **Память**. |
| ⏰ **Планировщик** | Задачи при включении ПК, по интервалу или ежедневно: запуск агента или озвучивание. |
| 🧱 **Minecraft** | Студия плагинов Paper/Spigot: генерация проекта из шаблонов и **компиляция в `.jar`** через Maven с живым логом. |
| 🖥️ **Серверы** | SSH-проводник по файловой системе удалённых серверов, редактор конфигов прямо на сервере, выполнение команд. |
| 🌐 **Перевод** | Перевод больших массивов данных и файлов (TXT/JSON/локализации) локальной моделью с разбивкой на части. |
| 🎙️ **Голос** | Голосовой ассистент (распознавание + синтез речи на русском). Горячая клавиша `Ctrl+Shift+Space`. |
| 💡 **Промпты** | Библиотека готовых задач в один клик — отправляются выбранному агенту. |
| 📚 **База знаний** | RAG-память на эмбеддингах Nomic: агенты подтягивают релевантные знания под запрос. |
| 🐝 **Команда (Swarm)** | Мультиагентные сценарии: координатор делит цель на подзадачи и распределяет их между специалистами. |
| 🧩 **Скилы** | Плагины-навыки агентов (безопасные шаблоны команд/HTTP), импорт/экспорт. |
| 📡 **Удалённый доступ** | Локальный сервер (в духе Claude dispatch): управляйте агентами с телефона в той же сети, с токеном. |
| ⚙️ **Настройки** | Язык интерфейса (по умолчанию English), тема, движок речи, песочница, RAG, и многое другое. |

> ⭐ Pro-режим временно отключён — все функции доступны бесплатно (см. [монетизацию](MONETIZATION.md) и [безопасность](SECURITY.md)).

### 🆕 Что нового в Mythera

- 🧠 **RAG-память (Nomic эмбеддинги)** — раздел «База знаний»: добавляйте заметки/файлы, агенты используют релевантные фрагменты.
- 🐝 **Swarm** — команда агентов решает сложную цель сообща (планировщик → исполнители → сведение).
- 🧩 **Плагины-скилы** — расширяйте агентов декларативными навыками; command-скилы проходят тот же фильтр безопасности.
- 👁️ **Компьютерное зрение** — инструмент `take_screenshot`: агент видит экран (для UI-агентов и vision-моделей llama3.2-vision / moondream).
- 🔨 **Forge/Fabric моды** — помимо плагинов, генерация и сборка модов через Gradle.
- 📡 **Удалённый доступ (dispatch)** — локальный режим сейчас; заложены облачный режим и мобильное приложение (веб-UI как основа PWA).
- 🎙️ **Голос лучше** — активация по имени («Mythera, …»), задаваемое имя ассистента, и переключение на **Piper (TTS)** + **Faster-Whisper (STT)** для оффлайн-качества вместо Windows Speech API.
- 🌍 **Язык по умолчанию — English**; многошаговые задачи до **40 шагов** (настраивается до 100).

### 🎯 Максимально дружелюбный интерфейс

- 🚀 **Онбординг-мастер** при первом запуске: приветствие → выбор сценариев → определение «железа» → установка моделей в один шаг → готово. ИИ работает за пару минут без настройки.
- ⌨️ **Командная палитра `Ctrl+K`** — мгновенный доступ к любому разделу и действию, как в VS Code.
- 🌍 **Мультиязычный интерфейс** — русский, English, Українська, Español, Deutsch, 中文 (переключение в настройках).
- 💽 **Выбор диска для нейросетей** — модели можно ставить на любой диск/папку (через `OLLAMA_MODELS`), с быстрым выбором среди доступных дисков и их свободного места.
- 🎬 **Сценарии использования** — готовые наборы агентов и автоматизаций в один клик (утренний помощник, контент-студия, рабочее место разработчика и др.).
- 🧩 **Галерея из 30+ агентов** и **40+ готовых промптов** с поиском.
- 🎨 **Темы оформления** — светлая/тёмная и 8 акцентных цветов.
- ⚙️ **Расширенные настройки** — автозапуск, старт в трее, уведомления, скорость речи, авто-прослушивание, креативность (temperature), глубина автономии, модель по умолчанию, полный доступ к диску, сброс настроек.
- 📊 **Дружелюбная загрузка моделей** — прогресс со скоростью, ETA и объёмом.
- 📤 **Импорт/экспорт агентов** файлом `.nexus.json`.

> ⭐ Pro-режим временно отключён — все функции доступны бесплатно и без ограничений.

### 🔐 Безопасность

- **Песочница файловой системы**: по умолчанию агенты работают только в рабочем пространстве; выход за его пределы и запись в системные каталоги (`Windows`, `Program Files`, `/etc` и др.) заблокированы. Полный доступ к диску включается явно в настройках.
- **Усиленный фильтр команд**: разрушительные операции (`format`, `diskpart`, `rm -rf`, `Remove-Item -Recurse`, fork-бомбы, `shutdown` и десятки других паттернов) блокируются регулярными выражениями, а не простым поиском подстроки.
- **Шифрование секретов**: пароли и SSH-ключи серверов шифруются через OS-хранилище (Electron `safeStorage`), а не лежат в открытом виде.
- **Изоляция окна**: запрет внешней навигации и всплывающих окон, открытие ссылок только в системном браузере и только по `http(s)`; `contextIsolation`, выключенный `nodeIntegration`, строгий CSP.

### 🧠 Умная долговременная память

Чтобы контекст «жил долго, но не забивался ненужным»:
- после каждого диалога агент **сам выделяет важные факты** (предпочтения, пути, серверы, договорённости) и сохраняет их;
- факты **дедуплицируются**, ранжируются по ценности и ограничены лимитом (мусор вытесняется);
- длинная история чата **сжимается в краткое резюме**, а свежие сообщения остаются как есть;
- память можно просмотреть, дополнить вручную или очистить (кнопка 🧠 в чате).

### 🧱 Студия плагинов Minecraft

Создаёт готовый Maven-проект (Paper API) из шаблонов *basic / command / listener*, генерирует `pom.xml`, `plugin.yml` и Java-класс, затем компилирует в `.jar`. Агент-разработчик умеет итеративно править код по логам ошибок сборки. Требуется JDK 17+ и Maven (приложение проверяет их наличие и подсказывает команды `winget`).

### 🖥️ Удалённые серверы (SSH/SFTP)

Подключения (пароль или приватный ключ) сохраняются локально. Доступны: навигация по каталогам, чтение и редактирование конфигов прямо на сервере, выполнение команд. Агент-администратор работает с серверами через инструменты `remote_*`.

---

## 🔬 Ресерч: какие нейросети выбраны и почему

Для обычного ПК ключевые критерии — **скорость**, **умеренные требования к ОЗУ** и **надёжный tool-calling** (без него агент не сможет управлять компьютером). На основе обзоров локальных моделей 2026 года отобраны:

| Модель | Размер | ОЗУ | Почему |
|---|---|---|---|
| **Llama 3.2 3B** (Meta) ★ | 2.0 ГБ | 8 ГБ | Быстрая, есть tool-calling — лучший «мозг» агента на слабом ПК. |
| **Qwen 2.5 7B** (Alibaba) ★ | 4.7 ГБ | 16 ГБ | Один из лучших открытых вариантов для агентов: стабильные вызовы инструментов. |
| **Gemma 2 9B** (Google) | 5.4 ГБ | 16 ГБ | Сильное качество ответов. |
| **Mistral 7B** | 4.1 ГБ | 16 ГБ | Очень быстрая, поддерживает инструменты. |
| **Phi 3.5 Mini** (Microsoft) | 2.2 ГБ | 8 ГБ | Работает даже на ноутбуках с 8 ГБ. |
| **Qwen 2.5 Coder 7B** | 4.7 ГБ | 16 ГБ | Лёгкий кодер для скриптов и быстрых задач. |
| **Nomic Embed** | 0.3 ГБ | 8 ГБ | Эмбеддинги для памяти/поиска по документам (RAG). |

### 🆕 Кодинг-модели 2026 (agentic, в духе Qwen Code CLI)

Для автономной работы над кодом (читать репозиторий → планировать → править много файлов → запускать тесты → итерировать) добавлены лучшие открытые модели июня 2026:

| Модель | Размер | ОЗУ | Почему |
|---|---|---|---|
| **Qwen3-Coder 30B (MoE)** (Alibaba) ★ | ~19 ГБ | 32 ГБ | Лучший открытый агентный кодер: RL-дообучен на SWE-Bench, нативный tool-calling, контекст до 256K. MoE (~3B активных) — быстрее плотных. Основа «Qwen-Code»-режима. |
| **Devstral Small 2 24B** (Mistral) ★ | ~14 ГБ | 24 ГБ | Создана под SWE-агентов (исследование кодовой базы, multi-file edits). ~68% SWE-bench Verified, Apache-2.0. |
| **DeepSeek-Coder V2 16B** | ~9 ГБ | 16 ГБ | Сильный кодоген и рассуждение, 338 языков. |
| **GLM-4 9B** (Z.ai) | ~5.5 ГБ | 16 ГБ | Хороша в terminal-agent и UI/веб-разработке; старшие GLM-5.x — для серверов. |

В приложении есть готовый агент **«Qwen-Code (CLI-агент)»** и сценарий **«Qwen-Code студия»**, которые предлагают скачать подходящую под ваше железо кодерскую модель и работают полностью автономно (write_file/create_directory/run_command/run_python + проверка тестами).

Приложение **само определяет объём ОЗУ** и предлагает набор: ≤8 ГБ → Llama 3.2 + Phi; 16 ГБ → Qwen 2.5 + Coder 7B; 17–32 ГБ → добавляет **Devstral**; 32+ ГБ → **Qwen3-Coder 30B**.

Источники ресерча: обзоры локальных кодинг-моделей и агентов июня 2026 (SWE-Bench/Terminal-Bench, Qwen3-Coder, Devstral, GLM-5.1, Kimi K2.6, DeepSeek V4; агенты Qwen Code CLI, Aider, OpenHands, Continue.dev).

---

## ❓ Вопросы и ответы (которые задают чаще всего)

**1. Это правда работает офлайн?**
Да. Модели крутятся на вашем ПК через Ollama. Интернет нужен только инструментам `web_search`/`http_get`, когда вы сами об этом просите.

**2. Безопасно ли давать ИИ доступ к PowerShell?**
Агент работает в каталоге-песочнице (`~/MytheraAI-Workspace`), а разрушительные команды (`format`, `shutdown`, `rm -rf /`, `diskpart`…) жёстко блокируются. Выполнение команд можно полностью отключить в настройках.

**3. Какой ПК нужен?**
Минимум 8 ГБ ОЗУ (модели 3B). Комфортно — 16 ГБ. Видеокарта ускоряет, но не обязательна — Ollama умеет работать на CPU.

**4. Чем это отличается от ChatGPT?**
Полная приватность (данные не уходят в облако), отсутствие подписки, и главное — агент реально **управляет вашим компьютером**, а не только пишет текст.

**5. Как агент «взаимодействует с компьютером»?**
Через набор инструментов: запуск команд, чтение/запись файлов, открытие приложений и URL, поиск в интернете, системная информация, уведомления. Модель сама решает, какой инструмент вызвать.

**6. Можно ли добавить свою модель?**
Да — установите любую модель Ollama, и она появится в списке выбора при настройке агента.

**7. Что значит «уровень автономии»?**
*Только чат* — без доступа к ПК. *Сбалансированный* — инструменты по необходимости. *Автономный* — многошаговые задачи (до 12 шагов) с самопроверкой.

**8. Голосовой ассистент на каком языке?**
Русский (`ru-RU`) для распознавания и синтеза, используется встроенный движок Windows.

---

## 💡 Идеи, которые реализованы

- ⚡ **Быстрая установка одной кнопкой** — ставит Ollama и подобранные под ПК модели.
- 🧩 **Конструктор агентов** — иконка, модель, характер (системный промпт), уровень автономии.
- 🛠️ **Живой просмотр действий агента** — в чате видно, какие инструменты он вызывает и что получает.
- 🔒 **Песочница и чёрный список команд** — безопасность по умолчанию.
- 📊 **Мониторинг системы** в реальном времени (CPU/RAM) в боковой панели.
- 🔔 **Системный трей** + сворачивание, работа в фоне.
- ⌨️ **Глобальная горячая клавиша** для голоса (`Ctrl+Shift+Space`).
- 🎨 **Стильный тёмный UI** с фирменным градиентом и кастомным заголовком окна.
- 🚀 **Автозапуск с Windows** и задачи «при включении».

- 🧠 **Умная долговременная память** — агент сам запоминает важное, сжимает старую историю, не копит мусор.
- 🧱 **Minecraft-студия** — генерация и компиляция плагинов Paper/Spigot в `.jar`.
- 🖥️ **SSH-проводник** по удалённым серверам с редактором конфигов.
- 🌐 **Пакетный переводчик** больших данных и файлов с чанкованием.
- 🤖 **Спец-агенты** — Minecraft-разработчик, серверный администратор, переводчик данных.

### Идеи на будущее (заложена архитектура)
RAG-память на эмбеддингах Nomic · мультиагентные сценарии (swarm) · плагины-«скилы» · экспорт/импорт агентов · компьютерное зрение (скриншоты экрана для UI-агентов) · поддержка Forge/Fabric-модов.

---

## 🚀 Запуск и сборка

```bash
npm install        # установка зависимостей
npm start          # запуск в режиме разработки
npm run icon       # сгенерировать иконку приложения (build/icon.ico, build/icon.png)
npm run dist:win   # компиляция: установщик (NSIS) + portable .exe → dist/
npm run dist:linux # сборка AppImage для Linux → dist/
```

Готовый `.exe` собирается автоматически в **GitHub Actions** (`.github/workflows/build.yml`) на каждом пуше — артефакт `nexus-ai-hub-windows`. При пуше тега `v*` установщик публикуется в **GitHub Release** (черновик). Фирменная иконка генерируется скриптом `scripts/gen-icon.js` без внешних зависимостей.

> 💰 Модель монетизации (Freemium / Pro / облачный мост / маркетплейс) описана в [MONETIZATION.md](MONETIZATION.md).

> Перед первым запуском агентов нажмите **«Быстрая установка»** на главном экране — приложение само поставит Ollama и нужные модели.

---

## 🏗️ Архитектура

```
src/
├── main/                 # Главный процесс Electron
│   ├── main.js           # окно, трей, IPC, горячие клавиши
│   ├── ollama.js         # клиент локального Ollama API (стриминг + tool-calling)
│   ├── installer.js      # установка Ollama, каталог и загрузка моделей
│   ├── agent.js          # движок автономных агентов (цикл модель↔инструменты)
│   ├── system.js         # инструменты управления ПК + системная информация
│   ├── scheduler.js      # планировщик задач (включение/интервал/расписание)
│   ├── voice.js          # координатор голосового ассистента
│   ├── memory.js         # умная долговременная память (факты + сжатие истории)
│   ├── minecraft.js      # студия плагинов Paper/Spigot + компиляция Maven
│   ├── remote.js         # SSH/SFTP: файлы, конфиги, команды на серверах
│   ├── translator.js     # перевод больших данных с разбивкой на чанки
│   ├── licensing.js      # тарифы Free/Pro, ключи, trial, гейтинг функций
│   ├── store.js          # JSON-хранилище конфигурации
│   └── preload.js        # безопасный мост contextBridge
└── renderer/             # Интерфейс
    ├── index.html
    ├── styles.css        # стильная тёмная тема
    └── app.js            # вся логика UI, чат, голос (Web Speech API)
```

Лицензия: MIT.
