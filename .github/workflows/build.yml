name: Build Windows App

on:
  push:
    branches: [ "claude/**", main ]
  workflow_dispatch:

jobs:
  build-windows:
    # Пинуем windows-2022 для воспроизводимости (windows-latest уезжает на
    # windows-2025 и может менять тулчейн). Actions — последние мажоры (v4),
    # это текущий способ уйти от устаревшего Node 20 в действиях.
    runs-on: windows-2022
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm install

      - name: Build Windows installer & portable
        run: npm run dist:win
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: mythera-ai-hub-windows
          path: |
            dist/*.exe
          if-no-files-found: warn

      - name: Publish installer to GitHub Release (on tag)
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: dist/*.exe
          draft: true
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
